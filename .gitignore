# Trained weights (downloaded separately, too large to track)
weights/hpl/
weights/survclam/
weights/*.tar*
weights/*.zip

# Weights-bundle build artifacts (produced by scripts/build_weights_bundle.sh)
weights_bundle/
pancolon_chips_weights.tar.gz

# Pipeline outputs / intermediates
work/
pipeline_work/
outputs/
results/
*.h5
*.h5ad
*.pt
*.svs
*.ndpi
*.tif
*.tiff
*.btf
*.mrxs
*.pkl

# Local config overrides (each collaborator copies pipeline.local.example.yaml
# to one of these and fills in their own paths -- never commit the filled-in
# version, it carries machine/lab-specific paths)
config/pipeline.local.yaml
config/pipeline.yaml

# Web app runtime artifacts
webapp/_run_config.yaml
webapp/_uploads/

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Logs
logs/
slurm-*.out
*.out
*.err

# OS
.DS_Store
